psycopg[binary]>=3.1
tiktoken>=0.7
numpy>=1.24

# Optional: only needed for the automatic Hugging Face download path in
# run_semantic_quality.py; a local --dataset-file needs neither.
# datasets>=2.14  # optional: HF download path for LongMemEval/LoCoMo
# huggingface_hub>=0.20  # optional: lighter alternative to datasets
